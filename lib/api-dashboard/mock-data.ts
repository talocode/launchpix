/** UI-only placeholders until billing meters ship. */

export const MOCK_METRICS = {
  availableCreditsUsd: "$0.00",
  totalSpentUsd: "$0.00",
  totalIssuedUsd: "$0.00",
  activeApiKeys: 0
};

export const MOCK_SETUP = {
  hasPaymentMethod: false,
  hasApiKey: false
};